using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Begin_07 : MonoBehaviour
{
    private void Awake()
    {
        print("Wake up first!");
    }
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Then start!");
    }
}
